{
  "name": "snu_ece_announcement",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "prepare:public": "rm -rf public && mkdir -p public/css public/js && cp index.html public/ && cp css/style.css public/css/ && cp js/*.js public/js/"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.100.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  }
}
